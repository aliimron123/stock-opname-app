import React from "react";
import { Input } from "./components/ui";

function App(): React.JSX.Element {
  return (
    <div className="inline-flex items-center justify-center h-screen">
      <Input />
    </div>
  );
}

export default App;
