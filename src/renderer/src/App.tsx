import { Button } from "./components/ui";

function App(): React.JSX.Element {
  return (
    <div className="h-screen inline-flex items-center justify-center">
      <Button className="mt-24">Click Me</Button>
    </div>
  );
}

export default App;
